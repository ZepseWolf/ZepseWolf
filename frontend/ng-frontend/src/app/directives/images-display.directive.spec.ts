import { ImagesDisplayDirective } from './images-display.directive';

describe('ImagesDisplayDirective', () => {
  it('should create an instance', () => {
    const directive = new ImagesDisplayDirective();
    expect(directive).toBeTruthy();
  });
});
