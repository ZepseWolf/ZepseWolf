import { SvgDisplayDirective } from './svg-display.directive';

describe('SvgDisplayDirective', () => {
  it('should create an instance', () => {
    const directive = new SvgDisplayDirective();
    expect(directive).toBeTruthy();
  });
});
